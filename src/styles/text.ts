import { StyleSheet } from "react-native";

const stylesText = StyleSheet.create({
    title: {
        fontSize: 32,
        fontWeight: 'bold',
        color: '#000'
    }
})

export default stylesText